export default function Page() {
  return (
    <div>
      <div className="admin-topbar"><h1>AcademicCalendar</h1></div>
      <div className="glass-card" style={{ padding: 24 }}>
        <p>This module isn't built out yet. It follows the same pattern as Placements and Syllabus: a Mongoose model, CRUD API routes under /api, and an admin page with a table + add/edit modal.</p>
      </div>
    </div>
  );
}
